# Atlas One v0.6 AI Integration

- [x] Create AI Provider system (Local, Remote, Mock)
- [x] 1. Register newly created managers (`PostProcessManager`, `MilitaryHUD`, `DetectionOverlay`, `SensorModePanel`) in `src/main.ts` and `src/ui/ui-manager.ts`.
- [x] 2. Implement state-based URL serialization/deserialization for the "Share Links" feature.
- [x] 3. Wire the `Toggle` commands (H, D) to their respective HUD systems.
- [x] 4. Verify system wiring and check for typescript errors.
- [x] Fix ESLint and Type errors in engine and providers
- [x] Fix ESLint errors in UI panel and tools
- [x] Verify API integration pattern

**Status: Complete**
